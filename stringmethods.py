# name = input("Enter your Full Name: ")
phone_number = input("Enter your phone number: ")
# result = len(name)
# result = name.find("d")   # it find the first occurance of whatever is inside the ()
# result = name.rfind("o")  # it find the last occurance of whatever is inside the ()

# name = name.capitalize()    # it capitalize the first letter of the string
# name = name.upper()         # it makes all the character into upper case
# name = name.lower()         # it makes all the character into lower case
# result = name.isdigit()     # it returns true only when the strings contains only digits otherwise it returns false
# result = name.isalpha()     # it return true only when the strings contains only alphabets else return false
# result = phone_number.count("-")   #it count the the seperators 
# phone_number = phone_number.replace("-","")   # it replaces the first value to the second value 

print(phone_number)