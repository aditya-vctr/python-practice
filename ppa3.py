# # Accept the comma-separated sequence from the user
# user_input = input()

# # Split the string into a list of strings based on the commas
# string_numbers = user_input.split(',')

# # Convert the list of strings into a list of actual integers
# integers = [int(num) for num in string_numbers]

# # Find and print the maximum value
# print(max(integers))

#Accept a sequence of comma-separated words as input. Reverse the sequence and print it as output.
# words = input().split(",")
# print(",".join(words[::-1]))
